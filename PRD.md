# Pi Code — 产品需求文档 (PRD)

**版本:** 1.0  
**状态:** 正式发布  
**最后更新:** 2026-07-27  

---

## 1. 产品概述

Pi Code 是一个 VS Code 扩展，将 [Pi 编程智能体](https://pi.dev) 以原生聊天面板的形式集成到 VS Code 中。用户可以用自然语言提出开发任务，Pi Code 自主完成"理解需求—检索代码—形成计划—修改代码—运行命令—验证结果—展示变更"的完整闭环。

### 1.1 产品定位

- **核心用户:** 需要在 VS Code 中使用 AI 编程辅助的软件开发者
- **差异化:** 非仅聊天/代码生成工具，而是具备完整工具调用、多模型支持、流式容错和工程安全机制的 Agent
- **依赖:** 需要安装 `pi` CLI (pi.dev)，Pi Code 作为其 VS Code 前端

### 1.2 核心价值

1. 原生 VS Code 体验 — 无需离开编辑器即可使用 AI 编程 Agent
2. 多会话并行 — 同时运行多个独立 Agent 会话
3. 模型灵活切换 — 支持多种 LLM Provider 并按需切换
4. 完整工具闭环 — Agent 能搜索、读取、编辑文件、执行命令、验证结果
5. 安全可控 — 多级权限体系、操作审计、可回滚

---

## 2. 用户故事

### 2.1 核心用户故事

| ID | 用户故事 | 优先级 |
| --- | --- | --- |
| US-01 | 作为开发者，我可以在 VS Code 中直接与 AI Agent 对话，无需离开编辑器 | P0 |
| US-02 | 作为开发者，我可以让 Agent 读取当前文件和选中的代码 | P0 |
| US-03 | 作为开发者，我可以让 Agent 搜索整个代码库中的文件和符号 | P0 |
| US-04 | 作为开发者，我可以让 Agent 生成修改计划并在批准后执行 | P0 |
| US-05 | 作为开发者，我可以审查 Agent 的每次代码修改并决定接受或拒绝 | P0 |
| US-06 | 作为开发者，我可以让 Agent 运行终端命令并查看实时输出 | P0 |
| US-07 | 作为开发者，我可以并行运行多个独立 Agent 会话 | P1 |
| US-08 | 作为开发者，我可以在不同 LLM 提供商和模型间切换 | P1 |
| US-09 | 作为开发者，我可以控制 Agent 的推理深度（思考级别） | P1 |
| US-10 | 作为开发者，我可以随时中断正在运行的 Agent 任务 | P0 |
| US-11 | 作为开发者，我可以回滚 Agent 的代码修改到操作前的状态 | P1 |
| US-12 | 作为开发者，重启 VS Code 后可以恢复之前的会话 | P1 |

### 2.2 进阶用户故事

| ID | 用户故事 | 优先级 |
| --- | --- | --- |
| US-13 | 作为开发者，我可以配置 Agent 的权限模式（只读/计划/手动/自动） | P1 |
| US-14 | 作为开发者，我可以创建和管理自定义技能与规则 | P2 |
| US-15 | 作为开发者，我可以使用 Git 工作流（创建分支、提交、PR） | P2 |
| US-16 | 作为开发者，我可以使用 @ 快速引用文件和符号 | P1 |
| US-17 | 作为开发者，我可以通过 / 命令快速操作 Agent | P1 |
| US-18 | 作为开发者，我可以将会话分叉为新的独立会话 | P1 |
| US-19 | 作为开发者，MCP Server 崩溃不影响主会话 | P2 |

---

## 3. 功能需求

### 3.1 聊天与上下文

| 功能 | 描述 | 优先级 |
| --- | --- | --- |
| 流式对话 | 模型文本、思考状态、工具调用实时显示 | P0 |
| 当前文件识别 | 自动获取活动编辑器和文件路径 | P0 |
| 选中代码引用 | 将选中代码及行号加入上下文 | P0 |
| 未保存内容读取 | 读取编辑器内存中的最新内容 | P0 |
| @文件名引用 | 支持模糊搜索和相对路径引用 | P1 |
| 行号引用 | 支持 @file.ts#L20-L45 精确引用 | P2 |
| 诊断上下文 | 读取 Problems 面板诊断信息 | P2 |
| Git 上下文 | 读取当前分支、未提交变更和 Diff | P1 |
| 上下文可见性 | 用户可查看实际附加了哪些上下文 | P1 |
| 上下文取消 | 发送前可移除任意上下文项 | P1 |

### 3.2 代码库探索

| 功能 | 描述 | 优先级 |
| --- | --- | --- |
| 文件枚举 | 按名称、后缀、目录、glob 查找 | P0 |
| 内容搜索 | 字面量和正则搜索 | P0 |
| 符号理解 | 获取类、函数、接口和引用关系 | P1 |
| 渐进读取 | 按任务逐步获取上下文，不一次性读取 | P0 |
| 二进制隔离 | 不读取二进制、依赖、构建产物 | P0 |
| 搜索可审计 | 界面可查看 Agent 搜索路径和结果 | P2 |
| 路径安全 | 路径规范化后访问，防止 ../ 越权 | P0 |

### 3.3 计划模式

| 功能 | 描述 | 优先级 |
| --- | --- | --- |
| 计划生成 | 复杂任务前生成结构化计划 | P0 |
| 只读探索 | 计划阶段只读不写 | P0 |
| 计划内容 | 目标、影响范围、修改文件、步骤、风险 | P0 |
| 计划审阅 | 以 Markdown 或专用界面展示 | P0 |
| 审批门禁 | 未经批准不得进入实施 | P0 |
| 计划变更 | 范围偏离时重新申请批准 | P1 |

### 3.4 文件修改与 Diff Review

| 功能 | 描述 | 优先级 |
| --- | --- | --- |
| 多文件修改 | 单任务可创建/修改/重命名/删除多文件 | P0 |
| 原生 Diff | 使用 VS Code Diff Viewer | P0 |
| 分级接受 | 支持整批/单文件/单修改块操作 | P1 |
| 修改前确认 | 手动模式需明确授权 | P1 |
| 用户二次编辑 | 用户可修改候选内容，Agent 读取真实版本 | P1 |
| 冲突检测 | 文件被修改后不得静默覆盖 | P1 |
| 原子写入 | 写文件失败不留下半个文件 | P0 |
| 自动打开文件 | Agent 编辑时自动打开定位文件 **(硬性要求)** | P0 |
| 修改高亮 | 当前编辑行有视觉提示 **(硬性要求)** | P0 |
| 防跳动策略 | 连续快速编辑时可关闭自动打开 | P1 |
| 最终变更汇总 | 完成后列出新增/修改/删除统计 | P1 |

### 3.5 终端与命令

| 功能 | 描述 | 优先级 |
| --- | --- | --- |
| 终端创建 | 在正确工作目录创建专用终端 | P0 |
| 命令预览 | 执行前显示命令、目录和风险 | P0 |
| 权限控制 | 询问/自动允许/禁止 | P0 |
| 输出捕获 | 结构化 stdout/stderr/退出码/时长 | P0 |
| 实时输出 | 长命令输出实时显示 | P0 |
| 中断命令 | 停止后终止进程及子进程 | P0 |
| 超时机制 | 可配置超时后清理进程 | P1 |
| Shell 兼容 | zsh/bash/PowerShell 均通过 | P1 |
| 敏感命令 | 删除/提权/强制 Git 等单独确认 | P1 |

### 3.6 Agent 智能体闭环

| 功能 | 描述 | 优先级 |
| --- | --- | --- |
| 多轮工具调用 | 单次响应可连续调用多工具 | P0 |
| 工具结果反馈 | 每次结果加入后续上下文 | P0 |
| 自动验证 | 修改后主动运行测试/build/lint | P1 |
| 失败修复 | 验证失败后定位原因并继续修复 | P1 |
| 死循环保护 | 相同工具/参数连续失败达阈值后停止 | P1 |
| 用户打断 | 任何阶段可停止或重新引导 | P0 |
| 状态可视化 | 显示当前阶段（搜索/计划/编辑/执行/验证） | P1 |
| 完成条件 | 结合文件状态和验证结果判定完成 | P0 |
| 未验证标记 | 未能测试时标记"未验证"及原因 | P2 |

### 3.7 权限体系

| 功能 | 描述 | 优先级 |
| --- | --- | --- |
| 只读模式 | 仅允许读取/搜索/分析 | P1 |
| 计划模式 | 可探索生成计划，不允许修改 | P1 |
| 手动模式 | 文件/命令/外部访问分别确认 | P1 |
| 自动编辑模式 | 普通文件自动执行，危险操作须确认 | P1 |
| Workspace Trust | 不可信工作区自动降级为只读 | P1 |

### 3.8 会话与检查点

| 功能 | 描述 | 优先级 |
| --- | --- | --- |
| 会话持久化 | 重启后恢复对话和工具记录 | P1 |
| 多会话 | 支持多个并行独立会话 | P0 |
| 检查点 | 每轮修改前后建立可识别检查点 | P1 |
| 回退代码 | 可回退文件但保留对话 | P1 |
| 对话分叉 | 从历史消息创建新会话分支 | P1 |
| 崩溃恢复 | 异常退出后恢复到最后一个一致状态 | P1 |

---

## 4. 非功能需求

### 4.1 性能

| 指标 | 目标 |
| --- | --- |
| 插件冷激活 P95 | ≤ 800 ms |
| 打开会话界面 P95 | ≤ 1.5 s |
| 空闲 CPU | 平均 ≤ 1% |
| 空闲增量内存 | ≤ 150 MB |
| 搜索首批结果 | ≤ 500 ms |
| 打开 Diff | ≤ 500 ms |
| 点击停止到任务停止 | ≤ 1 s |
| 终端命令停止 | ≤ 3 s |
| 并行会话 | ≥ 4 个不串流 |
| 连续任务稳定性 | 8 小时无 Extension Host 崩溃 |

### 4.2 安全

| 要求 | 描述 |
| --- | --- |
| 密钥存储 | API Key 仅保存在 VS Code SecretStorage |
| 配置脱敏 | settings.json/日志中无明文密钥 |
| CSP | Webview 配置严格 Content Security Policy |
| 路径边界 | 默认仅允许工作区根目录以内 |
| 命令注入防护 | 所有命令参数正确转义 |
| 审计日志 | 权限/文件/终端/网络/MCP 操作可审计 |

### 4.3 兼容性

| 平台 | 要求 |
| --- | --- |
| macOS Apple Silicon | 完整通过 |
| Windows 11 x64 | 完整通过 |
| Ubuntu 24.04 x64 | 完整通过 |
| VS Code ^1.85.0 | 完整支持 |
| Remote SSH/Dev Containers/WSL | 核心功能通过 |

---

## 5. 约束与限制

- Pi Code 是 Pi CLI 的 VS Code 前端，不包含独立 LLM 推理能力
- 需要 Node.js >= 18
- 需要 `pi` CLI 独立安装
- 终端集成需要 VS Code 集成终端
- 当前版本不支持内置 MCP Server（通过 pi CLI 间接支持）

---

## 6. 发布标准

参见 `交付标准.md` 第十一节（最终验收判定规则）。简言之：

- 所有 P0 功能完整实现并通过测试
- 不存在 Blocker/Critical/High 缺陷
- 不存在未修复的安全问题
- 不存在数据丢失风险
- 自动打开并定位编辑文件的功能完整通过（F-410/411/412）
- 源代码、VSIX、文档、测试报告和构建流程齐备
