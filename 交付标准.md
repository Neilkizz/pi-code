# VS Code 智能编程插件最终交付验收标准（v1.0）

## 一、最终交付目标

插件正式验收时，应达到以下产品定义：

> 用户可以直接在 VS Code 中用自然语言提出开发任务，插件能够自主完成“理解需求—检索代码—形成计划—申请权限—修改代码—运行命令—分析结果—修复问题—展示变更—提交结果”的完整闭环，并且所有文件修改、命令执行和外部访问均可审计、可中断、可回滚。

最终产品不得仅实现以下低阶形态：

- 只把用户问题发送给模型并显示文本回答；
- 只生成代码块，需要用户手工复制；
- 只能修改当前打开的单个文件；
- 只能调用固定模型或固定 API；
- 通过终端模拟输入输出，却没有结构化工具调用；
- 修改文件后无法查看 Diff、拒绝修改或回滚；
- 流式出错后重复拼接内容、重复执行工具或破坏会话状态。

---

## 二、建议采用的产品架构

正式版至少应包含五个相互解耦的模块：

| 模块 | 责任 |
|---|---|
| VS Code Extension Host | 调用 VS Code API，管理编辑器、文件、终端、诊断、Git 和权限 |
| Webview UI | 会话界面、计划界面、工具执行状态、权限确认、模型配置 |
| Agent Core | 智能体循环、上下文管理、计划、工具调度、结果验证 |
| Provider Adapter | Anthropic、OpenAI、Responses、OpenAI-compatible、CLIProxyAPI 等协议适配 |
| Persistence Layer | 会话、任务状态、检查点、配置、日志和统计数据 |

插件需要访问工作区内容和运行工作区命令，因此应以 Workspace Extension 为主，并正确处理本地、Remote SSH、Dev Container、WSL 和 Codespaces 中扩展宿主位置的差异。VS Code 官方将需要工作区访问的扩展定义为运行在工作区所在位置的扩展。citeturn158964view2turn212426view6

---

# 三、功能验收标准

## 3.1 安装、激活与基础界面

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| F-001 | 安装包 | 提供可直接安装的 `.vsix`，安装、升级、降级和卸载均不破坏 VS Code |
| F-002 | 激活方式 | 点击 Activity Bar、编辑器工具栏、状态栏或命令面板均可打开插件 |
| F-003 | 延迟激活 | 未使用插件时不得启动模型连接、扫描仓库或创建后台进程 |
| F-004 | 多位置显示 | 对话窗口可以位于主侧栏、辅助侧栏或编辑器标签页 |
| F-005 | 状态恢复 | 重启 VS Code 后恢复上次打开的会话标签和未完成任务状态 |
| F-006 | 主题适配 | Light、Dark、High Contrast 三种主题下均正常显示 |
| F-007 | 中文体验 | 中文输入、中文文件路径、中文输出和中文错误提示无乱码 |
| F-008 | 快捷键 | 支持聚焦输入框、新建会话、引用当前文件、停止任务等快捷键 |

Webview 应只在原生 VS Code API 无法满足复杂交互时使用，并且需要适配 VS Code 主题、键盘操作和高对比度模式。citeturn158964view1turn276737search4

---

## 3.2 对话与上下文获取

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| F-101 | 流式对话 | 模型文本、思考状态、工具调用和工具结果能够按事件实时显示 |
| F-102 | 当前文件 | 自动识别当前活动编辑器及文件路径 |
| F-103 | 当前选区 | 能把用户选中的代码及精确行号加入上下文 |
| F-104 | 未保存内容 | 读取编辑器内存中的最新内容，而不是只读取磁盘旧版本 |
| F-105 | 文件引用 | 支持 `@文件名`、相对路径和模糊搜索 |
| F-106 | 行号引用 | 支持类似 `@src/app.ts#20-45` 的精确引用 |
| F-107 | 目录引用 | 支持引用目录，并根据任务选择性读取文件 |
| F-108 | 拖入附件 | 支持把文件拖入输入框作为上下文 |
| F-109 | 诊断上下文 | 可以读取 Problems 面板中的错误、警告、语言服务诊断 |
| F-110 | Git 上下文 | 可以读取当前分支、未提交变更、暂存区和 Git Diff |
| F-111 | 多根工作区 | 多根 Workspace 下能够区分不同项目根目录 |
| F-112 | 忽略规则 | 默认遵守 `.gitignore`，并支持自定义排除 `.env`、密钥、构建目录等内容 |
| F-113 | 上下文可见性 | 用户能够看到本次请求实际附加了哪些文件、选区和诊断信息 |
| F-114 | 上下文取消 | 用户可以在发送前移除任意上下文项 |

Claude Code 当前会自动读取活动文件和选区，并支持带行号的文件引用；这应被视为基本能力而不是高级功能。citeturn945007view0turn945007view1

---

## 3.3 代码库探索能力

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| F-201 | 文件枚举 | 能按名称、后缀、目录和 glob 规则查找文件 |
| F-202 | 内容搜索 | 支持字面量和正则表达式代码搜索 |
| F-203 | 符号理解 | 能获取类、函数、接口、引用和定义关系 |
| F-204 | 渐进读取 | 不一次性把整个仓库发送给模型，而是按任务逐步获取上下文 |
| F-205 | 大仓库支持 | 10 万文件规模仓库中不会因全量扫描导致 VS Code 卡死 |
| F-206 | 二进制隔离 | 默认不读取二进制文件、依赖缓存、构建产物和超大文件 |
| F-207 | 搜索可审计 | UI 中能够看到 Agent 搜索过哪些路径和采用了哪些结果 |
| F-208 | 路径安全 | 路径规范化后才能访问，防止 `../` 和符号链接越权读取工作区之外内容 |

---

## 3.4 计划模式

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| F-301 | 计划生成 | 复杂任务修改文件前，能够生成结构化实施计划 |
| F-302 | 只读探索 | 计划阶段允许读取和搜索，但不得写文件或执行危险命令 |
| F-303 | 计划内容 | 至少包含目标、影响范围、修改文件、实施步骤、风险和验证方法 |
| F-304 | 计划审阅 | 计划以 Markdown 文档或专用界面打开 |
| F-305 | 用户批注 | 用户可以编辑计划或逐条添加意见 |
| F-306 | 审批门禁 | 未经用户批准，不得从计划阶段自动进入实施阶段 |
| F-307 | 计划变更 | 实施范围显著偏离原计划时，必须重新申请批准 |

Claude Code 的计划模式会先展示计划，并允许用户在完整 Markdown 文档中审阅和反馈，然后才开始修改。citeturn945007view1

---

## 3.5 文件修改与 Diff

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| F-401 | 多文件修改 | 单任务能够创建、修改、重命名和删除多个文件 |
| F-402 | 原生 Diff | 每次修改均可在 VS Code 原生 Diff Viewer 中查看 |
| F-403 | 分级接受 | 支持整批、单文件和单修改块接受或拒绝 |
| F-404 | 修改前确认 | 手动模式下，写入磁盘前必须获得明确授权 |
| F-405 | 用户二次编辑 | 用户可以直接修改 Agent 的候选内容，Agent 后续必须读取修改后的真实版本 |
| F-406 | 冲突检测 | 文件在生成修改后被用户或其他进程改变时，不得静默覆盖 |
| F-407 | 原子写入 | 写文件失败时不得留下半个文件或部分编码内容 |
| F-408 | 编码保持 | 保持原始换行符、UTF-8 编码和文件末尾换行约定 |
| F-409 | 格式化 | 修改后可调用语言格式化器，但必须记录该操作 |
| F-410 | 自动打开文件 | Agent 开始编辑某文件时，VS Code 自动打开该文件 |
| F-411 | 自动定位 | 编辑发生时自动滚动并定位到当前修改区域 |
| F-412 | 修改高亮 | 当前正在修改的行或代码块有明确视觉提示 |
| F-413 | 防跳动策略 | 连续快速修改多个文件时，自动打开行为可以关闭、暂停或设置为仅跟随当前文件 |
| F-414 | 最终变更汇总 | 任务完成后列出新增、修改、删除文件及代码行统计 |

**F-410、F-411、F-412 是本项目硬性要求。** 最终产品必须真正让 VS Code 自动展示 Agent 当前编辑的文件和位置，而不是只在聊天框中显示文件名，也不是只在任务结束后统一打开 Diff。

---

## 3.6 终端与命令执行

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| F-501 | 终端创建 | 能创建专用 VS Code Terminal，并在正确工作目录执行命令 |
| F-502 | 命令预览 | 执行前显示完整命令、工作目录和风险等级 |
| F-503 | 权限控制 | 根据权限模式决定询问、自动允许或禁止 |
| F-504 | 输出捕获 | 结构化获取 stdout、stderr、退出码和运行时长 |
| F-505 | 实时输出 | 长命令输出实时显示，不能等进程结束后一次性返回 |
| F-506 | 中断命令 | 点击停止后能终止当前进程及其子进程 |
| F-507 | 超时机制 | 每类命令可以配置超时，超时后清理残留进程 |
| F-508 | 大输出保护 | 超大日志使用截断、分页或写入文件，不得拖死 Webview |
| F-509 | Shell 兼容 | macOS zsh、Linux bash、Windows PowerShell 均通过测试 |
| F-510 | 退出码判断 | Agent 必须根据真实退出码判断成功或失败 |
| F-511 | 环境继承 | 可配置是否继承 VS Code、Shell 和工作区环境变量 |
| F-512 | 敏感命令 | 删除、提权、系统配置、Git 强制操作、网络脚本等必须单独确认 |
| F-513 | 命令审计 | 保存命令、时间、工作目录、退出码和授权来源 |

VS Code Shell Integration 能提供命令边界、退出码和当前工作目录等信息，可用于提升命令执行和结果验证的可靠性。citeturn158964view3

---

## 3.7 Agent 智能体闭环

智能体不得采用“模型返回什么就结束”的一次性调用方式。完整循环必须包含：

1. 收集上下文；
2. 分析任务；
3. 选择并调用工具；
4.读取工具结果；
5. 修改文件；
6. 执行构建、测试或静态检查；
7. 分析失败原因；
8. 必要时继续修复；
9. 汇总最终结果。

Claude Code 将这一机制概括为“收集上下文—采取行动—验证结果”的循环，工具结果会继续反馈给模型，直至完成任务或被用户中断。citeturn158964view5

具体验收要求：

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| F-601 | 多轮工具调用 | 单个模型响应可连续调用多个工具 |
| F-602 | 工具结果反馈 | 每次工具结果都加入后续模型上下文 |
| F-603 | 自动验证 | 修改后主动运行适当的测试、构建或 lint |
| F-604 | 失败修复 | 验证失败后能够定位原因并继续修复 |
| F-605 | 死循环保护 | 相同工具、相同参数连续失败达到阈值后停止并报告 |
| F-606 | 最大步数 | 会话可配置最大工具调用数、最大时间和最大费用 |
| F-607 | 用户打断 | 用户可以在任何阶段停止或重新引导 Agent |
| F-608 | 状态可视化 | UI 明确显示当前处于搜索、计划、编辑、执行还是验证阶段 |
| F-609 | 完成条件 | 不能仅以“模型声称完成”为依据，必须结合文件状态和验证结果 |
| F-610 | 未验证标记 | 未能执行测试时，最终结果明确标记“未验证”及原因 |

---

## 3.8 权限体系

正式版必须提供至少四种权限模式：

| 模式 | 行为 |
|---|---|
| 只读模式 | 只允许读取、搜索和分析 |
| 计划模式 | 可探索和生成计划，不允许修改文件 |
| 手动模式 | 文件修改、命令执行和外部访问分别确认 |
| 自动编辑模式 | 工作区内普通文件修改可自动执行，危险操作仍须确认 |

不得默认提供完全绕过权限的模式。确需提供时，必须隐藏在高级设置中，并显示持续风险警告。

权限至少按以下维度控制：

- 读取文件；
- 写入文件；
- 删除和重命名；
- 执行 Shell 命令；
- Git 写操作；
- 网络请求；
- MCP 工具；
- 工作区之外路径；
- 环境变量读取；
- 敏感文件读取。

VS Code Workspace Trust 专门用于防止打开不可信工作区时意外执行代码。插件在 Restricted Mode 下必须自动降级为只读或计划模式，不得执行工作区代码。citeturn212426view4turn895846search3

---

## 3.9 会话、检查点与回滚

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| F-701 | 会话持久化 | 重启 VS Code 后能够恢复完整对话和工具记录 |
| F-702 | 历史搜索 | 可按标题、关键词、项目和时间查找会话 |
| F-703 | 会话命名 | 自动生成标题，并允许用户重命名和删除 |
| F-704 | 多会话 | 同一工作区支持多个相互独立的并行会话 |
| F-705 | 检查点 | 每轮文件修改前后建立可识别的检查点 |
| F-706 | 仅回退代码 | 可以回退文件但保留对话 |
| F-707 | 对话分叉 | 可以从历史消息创建新会话分支 |
| F-708 | 分叉并回退 | 可以同时分叉对话并将代码恢复到对应状态 |
| F-709 | 用户修改保护 | 回退前检测用户在检查点之后进行的手工修改 |
| F-710 | 崩溃恢复 | 异常退出后能恢复到最后一个一致性状态 |
| F-711 | 删除清理 | 删除会话时同步清理临时文件和无引用检查点 |

检查点至少应达到 Claude Code 当前提供的三种语义：分叉会话、仅回退代码、分叉并回退代码。citeturn158964view4

---

## 3.10 Git 能力

正式验收至少支持：

- 查看 Git 状态和当前分支；
- 查看未提交和已暂存 Diff；
- 生成 Commit Message；
- 暂存指定文件；
- 创建 Commit；
- 创建和切换分支；
- 生成 Pull Request 描述；
- 在任何写操作前展示将执行的 Git 命令；
- 禁止未经确认执行 `reset --hard`、强制推送、清理未跟踪文件等破坏性操作；
- 支持 Git Worktree 作为并行任务的隔离机制。

Claude Code 已将提交、PR 和 Worktree 作为其 Git 工作流的一部分，因此正式对标版本不应只停留在“能读取 Git Diff”。citeturn945007view5

---

# 四、模型与 Provider 验收标准

## 4.1 必须支持的协议

正式版至少支持：

| 协议 | 最低能力 |
|---|---|
| Anthropic Messages API | 流式文本、工具调用、思考块、停止原因 |
| OpenAI Chat Completions | 流式文本、Function Calling、usage |
| OpenAI Responses API | 多事件流、工具调用、响应状态 |
| OpenAI-compatible | 自定义 Base URL、模型名和请求头 |
| CLIProxyAPI | 自定义端点、模型映射、凭证轮换及兼容性日志 |

每个 Provider Adapter 必须统一转换为内部标准事件：

- message-start；
- text-delta；
- reasoning-delta；
- tool-call-start；
- tool-call-delta；
- tool-call-end；
- tool-result；
- usage；
- message-end；
- error。

UI 和 Agent Core 不得直接依赖某个供应商的原始 SSE 格式。

---

## 4.2 配置能力

用户应能配置：

- Provider 名称；
- Base URL；
- API Key 或 OAuth；
- 模型 ID；
-协议类型；
-自定义 Header；
- 上下文窗口；
- 最大输出 Token；
- 请求超时；
- 首字节超时；
- 最大并发数；
- 重试次数；
- 代理；
- 模型别名；
- 主模型与备用模型；
- 是否支持图片、工具调用和推理内容。

配置至少分为：

- 全局配置；
-工作区配置；
- 单会话临时覆盖。

优先级必须明确且可预测：单会话 > 工作区 > 全局。

---

## 4.3 流式状态机硬性标准

这是最终验收中的核心质量门槛。

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| T-001 | 独立状态机 | 每个请求拥有独立的 Message、Content Block 和 Tool Call 状态 |
| T-002 | 生命周期校验 | `message_stop` 只能结束已开始的消息 |
| T-003 | 重复事件 | 重复或迟到的结束事件被幂等处理，不得导致插件崩溃 |
| T-004 | 工具参数拼接 | 按工具 ID 和索引拼接增量 JSON，完整解析后才能执行 |
| T-005 | 首包前重试 | 仅在尚未向用户输出任何内容、尚未执行工具时允许透明重试 |
| T-006 | 首包后断流 | 流已经开始后禁止切换凭证、模型或上游并继续拼流 |
| T-007 | 部分结果 | 断流后保留已收到内容，并明确标记响应未完整结束 |
| T-008 | 防重复执行 | 网络重试不得重复执行已完成的文件修改或终端命令 |
| T-009 | 停止传播 | 用户取消后立即停止读取流，并阻止后续工具调用 |
| T-010 | 协议异常 | 未知事件被记录但不得破坏整个会话 |
| T-011 | 空流处理 | 上游首个有效载荷前关闭时，返回明确的 `empty_stream` 类错误 |
| T-012 | 状态错误隔离 | 单个请求的状态错误不得污染其他并行会话 |

必须通过以下专项测试：

- 连续注入 1,000 组乱序、重复、截断和缺失 SSE 事件；
- 不得出现未捕获异常；
- 不得出现重复工具执行；
- 不得出现跨会话串流；
- 不得向用户暴露类似 `Received message_stop without a current message` 的内部状态错误；
- 首字节后网络断开时，绝不把第二个上游响应拼接到第一个响应后面。

---

## 4.4 降级与故障转移

允许故障转移的条件：

- DNS、连接或 TLS 在首字节前失败；
- 首字节前超时；
- 首字节前收到可重试的 429、500、502、503、529；
- 模型明确不可用且尚未开始响应。

禁止自动故障转移的条件：

- 已输出文本；
- 已输出思考内容；
- 已生成工具调用；
- 已执行文件或命令工具；
- 已收到 usage 或结束事件的一部分。

每次故障转移必须记录：

- 原 Provider 和模型；
- 错误类型；
- 是否已收到首字节；
- 重试目标；
- 重试次数；
- 最终结果。

---

# 五、MCP、规则和扩展能力

正式版至少支持：

## 5.1 MCP

- 配置 stdio、HTTP 或 SSE MCP Server；
- 启用、禁用、重连和删除；
- 展示服务器连接状态；
- 展示服务器提供的工具；
- 工具调用前适用同一权限体系；
- OAuth 和 API Key 不以明文存储；
- MCP Server 崩溃不得导致插件崩溃；
- 单个 MCP 调用可取消和超时。

VS Code 当前把扩展工具、内置工具和 MCP 工具都纳入 Agent 工具体系；语言模型本身只生成工具参数，实际执行必须由宿主受控完成。citeturn212426view2turn212426view3

## 5.2 项目规则与记忆

至少支持以下层级：

- 全局用户规则；
- 项目规则；
- 目录级规则；
- 会话临时规则。

规则需要显示来源，并且用户能够确认本次请求加载了哪些规则。项目规则应纳入版本控制，个人敏感规则不得默认写入项目。

## 5.3 Slash Commands、Skills 与 Hooks

正式版至少支持：

- 自定义 `/命令`；
- Prompt 模板；
- 可复用 Skill；
- 文件修改后格式化 Hook；
- 命令执行前校验 Hook；
- 任务结束后通知 Hook；
- Hook 超时和失败策略；
- 项目级与用户级作用域；
- 禁用单个 Hook；
- Hook 执行审计。

Hook 用于确定性地执行格式化、验证和项目规则，不应完全依赖模型自行决定是否执行。citeturn274956search19

## 5.4 子智能体

正式对标版至少提供：

- Explore：只读代码探索；
- Planner：实施方案设计；
- Implementer：文件修改；
- Reviewer：代码审查；
- Tester：测试和验证。

每个子智能体必须具有：

- 独立上下文；
- 明确的系统指令；
- 独立工具权限；
- 可单独选择模型；
- 最大步骤、超时和预算；
- 结构化结果返回；
- 全部活动可在主会话中查看。

子智能体不应直接互相修改同一工作区；并行写任务应使用 Git Worktree 或独立工作副本隔离。Claude Code 的子智能体同样采用独立上下文、工具和权限边界。citeturn274956search14

---

# 六、安全验收标准

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| S-001 | 密钥存储 | API Key、OAuth Token 只能保存在 VS Code SecretStorage 或系统密钥链 |
| S-002 | 配置脱敏 | 普通 `settings.json`、日志和会话文件中不得出现明文密钥 |
| S-003 | Workspace Trust | 不可信工作区自动进入只读模式 |
| S-004 | 路径边界 | 默认只允许访问工作区根目录以内 |
| S-005 | 敏感文件 | `.env`、私钥、凭证和密钥文件默认拒绝发送给模型 |
| S-006 | 命令注入 | 所有命令参数经过正确转义，不通过字符串拼接隐式执行 |
| S-007 | Webview CSP | 必须配置严格 Content Security Policy，禁止任意内联脚本 |
| S-008 | 输入净化 | 文件内容、路径和模型返回内容进入 HTML 前必须转义或净化 |
| S-009 | 网络白名单 | 新外部域名首次访问时要求授权 |
| S-010 | 日志脱敏 | Header、Key、Token、Cookie、环境变量和敏感路径自动脱敏 |
| S-011 | 遥测 | 默认不上传源代码、Prompt、Diff、命令输出和文件路径 |
| S-012 | 依赖安全 | 正式发布时不存在 Critical 或 High 级已知依赖漏洞 |
| S-013 | 权限旁路 | 模型输出不得通过文本诱导绕过工具权限层 |
| S-014 | Webview 隔离 | Webview 不能直接访问 Node.js、文件系统或密钥 |
| S-015 | 安全审计 | 提供权限、文件、终端、网络和 MCP 操作的审计日志 |

VS Code 提供加密的 `ExtensionContext.secrets` 用于保存敏感信息；Webview 则必须限制本地资源、采用 CSP、禁用不必要能力并净化所有来自工作区的输入。citeturn158964view0turn158964view1

---

# 七、性能与稳定性指标

以下指标均在发布构建、普通开发机和 10 万文件测试仓库中测量。

| 指标 | 通过标准 |
|---|---:|
| 插件冷激活 P95 | ≤ 800 ms |
| 打开会话界面 P95 | ≤ 1.5 s |
| 空闲 CPU | 平均 ≤ 1% |
| 空闲增量内存 | ≤ 150 MB |
| 单会话长时间运行内存 | ≤ 400 MB，且无持续线性增长 |
| 搜索普通仓库首批结果 | ≤ 500 ms |
| 搜索 10 万文件仓库首批结果 | ≤ 2 s |
| 打开 Diff | ≤ 500 ms |
| 自动打开并定位修改文件 | ≤ 300 ms |
| 点击停止到本地任务停止 | ≤ 1 s |
| 终端命令停止 | ≤ 3 s，包括子进程 |
| Webview 长文本滚动 | 无明显卡顿或整页冻结 |
| 并行会话 | 至少 4 个会话互不串流 |
| 连续任务稳定性 | 连续运行 8 小时无 Extension Host 崩溃 |
| 会话恢复 | 100 个历史会话下打开列表 ≤ 1 s |
| 大输出处理 | 10 MB 命令输出不导致界面假死 |
| 异常恢复 | Provider 断连后插件无需重载即可继续新会话 |

VS Code 通过 Extension Host 隔离扩展，并要求扩展延迟激活，避免影响启动和 UI 操作；本项目的性能指标应在这一原则上进一步量化。citeturn158964view2

---

# 八、兼容性验收矩阵

正式版必须通过：

| 环境 | 验收要求 |
|---|---|
| macOS Apple Silicon | 必须完整通过，作为首要平台 |
| Windows 11 x64 | 必须完整通过 |
| Ubuntu 24.04 x64 | 必须完整通过 |
| VS Code Stable | 当前 Stable 和前两个稳定版本 |
| VS Code Insiders | 核心功能无阻断性错误 |
| 单文件模式 | 可聊天和引用文件，禁止工作区级危险操作 |
| 单根 Workspace | 完整支持 |
| Multi-root Workspace | 完整支持 |
| Remote SSH | 文件和命令在远端工作区正确执行 |
| Dev Containers | 文件、终端和 Provider 配置位置正确 |
| WSL | Windows UI 与 WSL 工作区路径不混淆 |

`package.json` 必须通过 `engines.vscode` 明确兼容版本；远程环境中还必须验证扩展运行位置和 `localhost` 指向，不能把远端 CLIProxyAPI 与本机 CLIProxyAPI 混淆。VS Code 官方要求扩展显式声明版本兼容性，并针对 Remote Development 的扩展宿主差异进行适配。citeturn212426view6turn212426view7

---

# 九、测试验收标准

## 9.1 自动化测试

| 测试类型 | 通过标准 |
|---|---|
| 单元测试 | 总体覆盖率 ≥ 80% |
| Agent Core 单元测试 | 分支覆盖率 ≥ 90% |
| SSE 解析器测试 | 分支覆盖率 ≥ 95% |
| 权限模块测试 | 分支覆盖率 ≥ 95% |
| Provider Contract Test | 每个 Provider 全部通过统一协议测试 |
| VS Code 集成测试 | 在 Extension Development Host 中自动运行 |
| 端到端测试 | 所有关键场景 100% 通过 |
| 故障注入测试 | 429、5xx、超时、截断、乱序、空流全部覆盖 |
| 性能测试 | 达到第七章指标 |
| 安全测试 | 不存在未解决 Critical/High 问题 |
| 稳定性测试 | 8 小时压力运行无崩溃和明显泄漏 |

VS Code 官方提供 Extension Development Host，用于在真实 VS Code API 环境中运行扩展集成测试。citeturn212426view5

## 9.2 必须通过的端到端场景

1. 选中一段代码，插件准确引用文件和行号并解释逻辑。
2. 要求实现一个跨三个文件的功能，先生成计划，批准后完成修改。
3. Agent 修改文件时，VS Code 自动打开目标文件并定位到修改行。
4. 用户拒绝其中一个 Diff Hunk，其余修改仍可正常接受。
5. 用户在候选 Diff 中手工修改代码，Agent 后续读取到用户修改后的版本。
6. Agent 运行测试，测试失败后自动分析并修复，再次运行后通过。
7. 执行危险命令时必须弹出权限确认，拒绝后不得以其他方式绕过。
8. 流开始前出现 503，自动切换备用模型并成功完成。
9. 流开始后连接中断，保留部分结果但不得拼接备用模型响应。
10. 工具调用 JSON 被拆成多个 SSE Chunk，最终只执行一次。
11. 用户点击停止后，模型流、工具调用和终端子进程全部停止。
12. VS Code 异常退出后重新打开，恢复会话和最后一致检查点。
13. 回退检查点后，Agent 修改恢复，但用户后续手工修改不被静默覆盖。
14. 同时运行四个会话，不发生消息、工具调用或文件状态串线。
15. 打开不可信仓库时，插件不得执行命令或自动修改文件。
16. API Key 不出现在设置文件、日志、异常栈和导出的会话中。
17. CLIProxyAPI 返回异常 `message_stop` 时，插件不崩溃且能继续新请求。
18. Remote SSH 中执行命令时，命令必须运行在远端而不是本机。
19. MCP Server 崩溃时，当前调用失败，但主会话保持可用。
20. 10 万文件仓库中执行代码搜索，VS Code UI 不冻结。

---

# 十、交付物验收

最终交付必须同时提供：

1. 完整源代码；
2. 可安装 `.vsix`；
3. `README.md`；
4. `PRD.md`；
5. `TECH_DESIGN.md`；
6. `ROADMAP.md`；
7. `TEST_PLAN.md`；
8. `SECURITY.md`；
9. Provider 接入规范；
10. Tool 接口规范；
11. 配置项说明和 JSON Schema；
12. 架构图和模块依赖图；
13. 数据库或本地存储结构说明；
14. 自动化测试报告；
15. 性能测试报告；
16. 安全扫描报告；
17. 系统兼容性测试矩阵；
18. 已知问题清单；
19. `CHANGELOG.md`；
20. CI/CD 发布流程；
21. Marketplace 图标、说明页和隐私说明；
22. 可重复构建脚本。

扩展应能使用 `vsce package` 生成 VSIX，并具备发布到 VS Code Marketplace 或通过 VSIX 私下分发的能力。citeturn212426view7

---

# 十一、最终验收判定规则

只有同时满足以下条件，才判定为“正式验收通过”：

## 硬性通过条件

- 本文所有标记为正式版要求的功能全部实现；
- 所有关键端到端场景 100% 通过；
- 不存在 Blocker 或 Critical 缺陷；
- 不存在未修复的 High 级安全问题；
- 不存在已知的数据丢失问题；
- 不存在未经授权执行命令或修改文件的问题；
- 不存在跨会话串流或跨会话工具调用；
- 不存在流式重试导致的重复工具执行；
- 自动打开并定位 Agent 当前编辑文件功能完整通过；
- 三个平台和目标 VS Code 版本兼容性全部通过；
- 源代码、VSIX、文档、测试报告和构建流程全部齐备。

## 缺陷容许范围

正式交付时最多允许：

- 0 个 Blocker；
- 0 个 Critical；
- 0 个 High 安全缺陷；
- 不超过 5 个不影响主流程的 Medium 缺陷；
- Low 级体验问题可进入后续版本，但必须登记在已知问题清单中。

## 一票否决项

出现以下任意一项，直接判定验收不通过：

- Agent 未经许可修改文件或执行命令；
- API Key 或 Token 明文泄漏；
- 用户点击停止后仍继续修改文件；
- 回滚导致用户文件丢失；
- 上游断流后把不同请求内容拼接为同一回答；
- 同一工具因重试被执行两次；
- Agent 修改文件时 VS Code 无法打开并定位到修改位置；
- 插件频繁导致 Extension Host 崩溃或 VS Code 卡死；
- 测试报告和实际构建版本不一致；
- 只能在开发环境运行，无法生成可安装 VSIX。

---

# 十二、产品成熟度结论

达到本标准后，产品应具备以下定位：

> 一个可独立使用、支持多模型和自定义代理、具备完整工具闭环和工程安全机制的 VS Code 智能编程 Agent。其核心能力达到 Claude Code VS Code 插件同类产品的基本成熟度，同时在 CLIProxyAPI、多端点路由、模型回退、流式容错和当前编辑位置可视化方面形成自身差异化能力。

仅完成聊天、代码生成和简单文件写入，最多只能视为技术原型；只有完整通过本验收标准，才可视为可正式发布的 v1.0 产品。